# React Frontend (MERN)

## Setup

```bash
cd frontend-react
npm install
npm run dev
```

Opens at http://localhost:5173

The Vite proxy forwards `/api` requests to `http://localhost:3000` (backend).
Make sure the backend is running first: `cd backend && npm run dev`
